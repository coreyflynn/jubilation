// @flow
import React from 'react';

type Props = {
  data: Point[],
};
type Context = { JubilationContext: JubilationContext };
export default function JubilationScatter(props: Props, context: Context): React.Element<*> {
  return <g />;
}
